<!doctype html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">
        <meta name="mobile-web-app-capable" content="yes">
        <meta name="apple-mobile-web-app-capable" content="yes">
        <meta name="application-name" content="{{@blog.title}}">
        <meta name="theme-color" content="#000000">
        <link rel="stylesheet" href="{{asset "css/shade.css"}}">
        {{! Ghost outputs important style and meta data with this tag }}
        {{ghost_head}}
    </head>
    <body class="{{body_class}}">
        {{! Everything else gets inserted here }}
        {{{body}}}

        <footer>
            <div class="container">
                <div class="row">
                    <div class="md-10 offset-md-1">
                        <div class="footer">&copy; 2017 <a href="/">{{@blog.title}}</a>. Theme: Shade by <a href="https://github.com/moso">Morten Sørensen</a>. Published with <a href="https://ghost.org">Ghost</a>.</div>
                    </div>
                </div>
            </div>
        </footer>

        <script>var max_pages = parseInt('{{pagination.pages}}');</script>
        <script src="{{asset "js/manifest.js"}}"></script>
        <script src="{{asset "js/vendor.js"}}"></script>
        <script src="{{asset "js/shade.js"}}"></script>
        {{! Ghost outputs important scripts and data with this tag }}
        {{ghost_foot}}
    </body>
</html>
